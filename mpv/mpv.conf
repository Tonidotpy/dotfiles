#########
# Video #
#########

vo=gpu                                      # Video output backend
#vd=                                        # Priority list of video decoders
#vf=                                        # List of video filters

#untimed                                    # Do not sleep when outputting video frames
#framedrop=vo                               # Skip frames to maintain A/V sync
#video-latency-hacks=no                     # Hacks to reduce video latency
#override-display-fps=                      # Set the display FPS

hwdec=vaapi                                 # Hardware video decoding API
#gpu-hwdec-interop=auto                     # Hwdec interop troubleshooting
#hwdec-extra-frames=                        # Number of GPU frames hardware decoding should preallocate
#hwdec-image-format=                        # Internal hardware decoder pixel format
#cuda-decode-device=                        # Choose the GPU used for decoding when using 'cuda' or 'nvdec' hwdec
#vaapi-device=/dev/dri/renderD128           # Choose the DRM device for 'vaapi-copy'

#panscan=                                   # Pan-and-scan
#video-aspect-override=-1                   # Override video aspect ratio
#video-aspect-method=container              # Default video aspect determination method
#video-unscaled=                            # Scaling of the video
#video-pan-x=                               # Move video rectangle by a percentage of the total size in the X direction
#video-pan-y=                               #       ~~~                                                     Y direction
#video-rotate=                              # Rotate video clockwise
#video-zoom=                                # Adjust video display scale factor
#video-scale-x=                             # Multiply video display size
#video-scale-y=                             #       ~~~
#video-align-x=                             # Moves video rectangle within the black borders
#video-align-y=                             #       ~~~
#video-margin-ratio-left=0                  # Extra border margin
#video-margin-ratio-right=0                 #       ~~~
#video-margin-ratio-top=0                   #       ~~~
#video-margin-ratio-bottom=0                #       ~~~

#no-correct-pts                             # Switches mpv to fixed framerate value mode
#fps=60.0                                   # Override video framerate
#deinterlace=no                             # Interlacing
#frames=                                    # Play only the first 'n' frames an then quit

#video-output-levels=auto                   # RGB color levels used
#hwdec-codecs=                              # List of allowed codecs for hardware decoding

#vd-lavc-check-hw-profile=yes               # Check hardware decoder profile
#vd-lavc-software-fallback=3                # Fallback to software decoding
#vd-lavc-film-grain=auto                    # Film grain application
#vd-lavc-dr=yes                             # Direct rendering
#vd-lavc-bitexact                           # Use bit-exact algorithms in during decoding (for codec testing)
#vd-lavc-fast                               # Unsafe optimizations for MPEG-1/2 and H.264 only
#vd-lavc-o=                                 # Pass AVOption to libavcodec decoder
#vd-lavc-show-all=no                        # Show broken/corrupted frames
#vd-lavc-skiploopfilter=default             # Skips the loop filter during decoding (H.264, HVEC only)
#vd-lavc-skipidct=                          # Skips the IDCT step (MPEG-1/2/4 only)
#vd-lavc-skipframe=                         # Skips decoding of frames
vd-lavc-threads=8                           # Threads for decoding
#vd-lavc-assume-old-x264=no                 # Assume video decoded with old version of x264

#swapchain-depth=3                          # Frame latency


#########
# Audio #
#########

#audio-pitch-correction=yes                 # Automatic pitch correction with different video speed
#audio-device=auto                          # Audio device
audio-exclusive=no                          # Exclusive output mode
audio-fallback-to-null=yes                  # If no audio device if found behave as ao=null

ao=pulse                                    # Audio output drivers
#af=                                        # List of audio filters
audio-spdif=dts-hd                          # List of codecs for which compressed audio passthrough should be used
#ad=                                        # Priority list of audio decoders

#volume=100                                 # Volume
replaygain=track                            # Adjust volume gain based on metadata
#replaygain-preamp=0                        # Pre-amplification gain
#replaygain-clip=yes                        # Prevent audio clipping
#replaygain-fallback=                       # Gain to apply if not specified in metadata

#audio-delay=                               # Audio delay
#mute=no                                    # Mute at startup
#audio-demuxer=                             # Audio demuxer

#ad-lavc-ac3drc=0                           # Dynamic Range Compression for AC-3 audio streams
#ad-lavc-downmix=no                         # Audio channel downmixing
#ad-lavc-threads=0                          # Threads to use for decoding
#ad-lavc-o=                                 # AVOption to pass to libavcodec decoder

#audio-channels=                            # Output audio channels
#audio-display=embedded-first               # Display cover art when playing audio files
#audio-files=                               # Play external audio files during video
#audio-file=                                #       ~~~           file during video
#audio-format=                              # Audio sample format
#audio-samplerate=                          # Audio output sample rate

#gapless-audio=weak                         # Play consecutive audio files with no silence or disruption
initial-audio-sync                          # Sync audio with video at the start of an event (play, seek, ecc...)
#volume-max=130                             # Maximum amplification level
#audio-file-auto=no                         # Load additional audio files matching the video filename
#audio-file-paths=                          # List of auto-loaded audio files
#audio-client-name=                         # Application name the player reports to audio API
#audio-buffer=                              # Audio output minimum buffer
#audio-stream-silence=no                    # Do not stop and restart audio on seeks and fill with silence
#audio-wait-open=                           # Wait 'n' seconds before sending data to audio device


###################
# Track selection #
###################

alang=it,ita,en,eng                         # Priority list of audio languages
slang=it,ita,en,eng                         #        ~~~       subtitles languages
vlang=it,ita,en,eng                         #        ~~~       video languages

#aid=auto                                   # Audio track selection
#sid=auto                                   # Subtitle stream selection
#vid=auto                                   # Video channel selection

#edition=auto                               # Set of chapters to use (mkv only)
#track-auto-selection=yes                   # Default track auto-selection (alang, aid, ecc...)
subs-with-matching-audio=no                 # Prefer a non-forced subtitle track over the forced one


####################
# Playback control #
####################

#start=                                     # Start at given time
#end=                                       # Stop at given time
#length=                                    # Stop at given time relative to the start
#rebase-start-time=yes                      # Wheter to move the file start time to 00:00:00
#speed=1.0                                  # Playback speed

#pause                                      # Start player in paused state
#shuffle                                    # Play files in random order

#playlist-start=auto                        # Set which file on the internal playlist to start playback with
#playlist=                                  # Play files according to a playlist files
#load-unsafe-playlist=no                    # Load URLs from playlists which are considered unsafe

#chapter-merge-threshold=100                # Threshold for merging almost consecutive ordered chapter parts in milliseconds
#chapter-seek-threshold=5.0                 # DIstance in seconds from the beggining of a chapther within which a backward chapter seek will go to the previous chapter
#hr-seek=default                            # Select when to use precise seek that are not limited to keyframes
#hr-seek-demuxer-offset=                    # Workaround for possible failures of the precise seek
#hr-seek-framedrop=yes                      # Allow the video decoder to drop frames during seek
#index=default                              # Controls how to seek in files
#access-references=yes                      # Follow any references in the file being opened

#loop-playlist=1                            # Loops playback
#loop-file=                                 # Loop a single file
#ab-loop-a=                                 # Set loop points
#ab-loop-b=                                 #      ~~~
#ab-loop-count=inf                          # Run A-B loops multiple times

#ordered-chapters                           # Support for mkv ordered chapters
#ordered-chapters-files=                    # Loads the given file as a playlist and tries to use the files contained in it as reference files when opening a mkv file that uses ordered chapters
#chapters-file=                             # Load chapters from this file instead of using the chapter metadata found in the main file

#sstep=                                     # Skip the amount of seconds specified after every frame
#stop-playback-on-init-failure=no           # Stop playback if either audio or video fails to initialize

#play-dir=forward                           # Control the playback direction
#video-reversal-buffer=                     # Control the amounts of bytes that can be used for backwards decoding
#audio-reversal-buffer=                     #       ~~~
#video-backward-overlap=auto                # Number of overlapping keyframe ranges to use for backwards decoding
#audio-backward-overlap=auto                #       ~~~
#video-backward-batch=1                     # Number of keyframe ranges to decode at once when backward decoding
#audio-backward-batch=10                    #       ~~~
#demuxer-backward-playback-step=60          # Number of seconds the demuxer should seek back to get new packets during backward playback


####################
# Program behavior #
####################

#idle=no                                    # Makes mpv wait instead of quitting when there is no file to play
#load-scripts=yes                           # Automatically loads scripts from the 'scripts' subdirectory
#scripts=                                   # Loads multiple scripts
#script-opts=                               # Set options for scripts
#reset-on-next-file=                        # Reset the values of the listed options on playback start to the initial value

ytdl                                        # The youtube-dl hook-script
#ytdl-format=bestvideo+bestaudio/best       # Video format/quality that is directly passed to youtube-dl
#ytdl-raw-options=                          # Arbitrary options for youtube-dl

#load-stats-overlay=yes                     # Enable the built-in script that shows useful playback information
#load-osd-console=yes                       # Enable the built-in script that shows the console
#load-auto-profiles=auto                    # Enable the built-in script that does auto profiles
#player-operation-mode=cplayer              # "Pseudo GUI mode", this option should not normally be used directly


###############
# Watch later #
###############

save-position-on-quit                       # Always save the current playback position on quit
#watch-later-directory=                     # Directory in which to store the 'watch later' temporary files
#no-resume-playback                         # Do not restore playback position from the 'watch_later' configuration subdirectory
#resume-playback-check-mtime                # Only restore the playback position from the 'watch_later' configuration subdirectory if the file's modification time is the same as the time of saving
#watch-later-options=                       # The options that are saved in 'watch later' files
#write-filename-in-watch-later-config       # Prepend the watch later config files with the name of the file they refer to
#ignore-path-in-watch-later-config          # Ignore path when using watch later feature


#############
# Subtitles #
#############

#sub-demuxer=                               # Subtitle demuxer
#sub-delay=                                 # Delays subtitles
#sub-files=                                 # List of subtitles files
#sub-file=                                  #       ~~~         file

#secondary-sid=                             # Secondary subtitle stream
#sub-scale=1                                # Subtitle font size factor
#sub-scale-by-window=yes                    # Scale subtitles on window resize
#sub-scale-with-window=yes                  # Subtitle size relative to window size
#sub-ass-scale-with-window=no               #       ~~~                             (ASS format only)

#embeddedfonts=yes                          # Use embedded mkv and ASS font
#sub-pos=100                                # Subtitles vertical position from the top
#sub-speed=                                 # Subtitles speed multiplier

#sub-ass-force-style=                       # Override subtitles style or script parameters
#sub-ass-hinting=none                       # Font hinting type
#sub-ass-line-spacing=                      # Line spacing for SSA/ASS renderer
#sub-ass-shaper=complex                     # Text layout engine
#sub-ass-styles=                            # SSA/ASS style file
#sub-ass-override=yes                       # Apply styles override
#sub-ass-force-margins=no                   # Subtitles on black bars
#sub-use-margins=yes                        #       ~~~
#sub-ass-vsfilter-aspect-compat=yes         # Stretch SSA/ASS subtytles with anamorphic videos
#sub-ass-vsfilter-blur-compat=yes           # Scale blur tags by video resolution
#sub-ass-vsfilter-color-compat=basic        # Mangle colors like (xy-)vsfilter do

#stretch-dvd-subs=no                        # Stretch DVD subtitles
#stretch-image-subs-to-screen=no            # Stretch DVD and other image subtitles to the screen
#image-subs-video-resolution=no             # Override image subtitle resolution with video resolution

#sub-ass                                    # Render ASS subtitles natively
sub-auto=fuzzy                              # Load additional subtitles files matching video filename
#sub-codepage=                              # Subtitles codepage
#sub-fix-timing=                            # Adjust subtitle timing
#sub-forced-only=auto                       # Display only forced subtitles
#sub-fps=                                   # Subtitles framerate
#sub-gauss=0                                # Subtitles blur
#sub-gray=                                  # Convert image subtitles to grayscale
#sub-file-paths=                            # List of directories to search for subtitles
#sub-visibility                             # Show subtitles
#secondary-sub-visibility                   # Show secondary subtitles
#sub-clear-on-seek                          # Clear subtitles on seek (rarely usefull)
#teletext-page=                             # For dvb_teletex subtitle streams
#sub-past-video-end                         # Update subtitles based on audio after last video frame

#sub-font=                                  # Subtitles font
#sub-font-size=55                           # Subtitles font size
#sub-back-color=                            # Subtitles background-color
#sub-blur=                                  # Subtitles blur
#sub-bold=                                  # Make subtitles bold
#sub-italic=                                # Make subtitles italic
#sub-border-color=                          # Subtitles border color
#sub-border-size=3                          # Subtitles border size
#sub-color=                                 # Subtitles color
#sub-margin-x=25                            # Subtitles horizontal screen margin
#sub-margin-y=22                            # Subtitles vertical screen margin
#sub-align-x=center                         # Subtitles horizontal alignment
#sub-align-y=bottom                         # Subtitles vertical alignment
#sub-justify=no                             # Justify subtitles
#sub-shadow-color=                          # Subtitles shadow color
#sub-shadow-offset=0                        # Subtitles shadow offset
#sub-spacing=0                              # Subtitles spacing

#sub-filter-sdh=no                          # Subtitles filter for the deaf or hard-of-hearing (SDH)
#sub-filter-sdh-harder=no                   #           ~~~
#sub-filter-regex-append=                   # List of regex to match on text subtitles
#sub-filter-regex-plain=no                  # Convert ASS "text" to plain-text
#sub-filter-regex-warn=no                   # Warning log level for regex
#sub-filter-regex-enable=yes                # Regex filtering

#sub-create-cc-track=no                     # Create a close captions track
#sub-font-provider-auto=auto                # Libass font provider backend


##########
# Window #
##########

#title=                                     # Window title
#screen=default                             # Screen to display the video on
#screen-name=                               #       ~~~

fullscreen                                  # Fullscreen
#fs-screen=current                          # Screen to display the fullscreen video on
#fs-screen-name=                            #       ~~~

#keep-open=no                               # Do not quit when file ends
#keep-open-pause=yes                        # Pause after keep open

image-display-duration=3                    # Image display duration
force-window=yes                            # Create window even if there is no video
#ontop                                      # Keep player on top of other windows
#border                                     # Window border
#on-all-workspaces                          # Show window on all workspaces

#geometry=                                  # Window geometry
#autofit=                                   # Initial window size
#autofit-larger=                            #       ~~~           only if window would be larger
#autofit-smaller=                           #       ~~~           only if window would be smaller
#window-scale=                              # Window resize multiplier
#window-minimized=                          # Minimize window
#window-maximized=                          # Maximize window

cursor-autohide=1000                        # Hide cursor
cursor-autohide-fs-only                     #       ~~~   only in fullscreen
#fixed-vo=yes                               # Do not enforce closing and reopening the video window for multiple files
#force-rgba-osd-rendering                   # Change video outputs render for OSD and text subtitles
#force-window-position                      # Force window default location
keepaspect                                  # Keep video aspect ratio
no-keepaspect-window                        #       ~~~               by resizing the window
#monitoraspect=                             # Set monitor aspect ratio
#hidpi-window-scale                         # Scale window size according to the backing scale factor
#monitorpixelaspect=1                       # Set pixel aspect ratio
#stop-screensaver=yes                       # Turns off screensaver
#wid=                                       # Attach to an existing window
#no-window-dragging                         # Disable moving the window with the mouse

#x11-name                                   # Set window class name
#x11-netwm=auto                             # Control the use of NetWM protocol features
#x11-bypass-compositor=fs-only              # Ask compositor to unredirect the window
#x11-present=auto                           # Use presentation statistics from X11's presentation extension


################
# Disc devices #
################

#cdrom-device=/dev/cdrom                    # CD-ROM device
#dvd-device=/dev/dvd                        # DVD device
#bluray-device=                             # Blu-ray disc location

#cdda-speed=                                # CD spin speed
#cdda-paranoia=0                            # Paranoia level
#cdda-sector-size=                          # Atomic read size
#cdda-overlap=                              # Force minimum overlap search to specified sector
#cdda-toc-bias                              # Assume beginning offset will be addressed as LBA 0
#cdda-toc-offset=                           # Add sectors to the values reported when addressing tracks
#cdda-skip=                                 # Skip imperfect data reconstruction
#cdda-cdtext=no                             # Print CD text

#dvd-speed=0                                # Limit DVD speed
#dvd-angle=1                                # Angle to view


#############
# Equalizer #
#############

#brightness=0                               # Brightness
contrast=5                                  # Contrast
#saturation=0                               # Saturation
#gamma=0                                    # Gamma
#hue=0                                      # Hue


###########
# Demuxer #
###########

#demuxer=                                   # Demuxer type
#demuxer-lavf-analyzeduration=              # Maximum time to analyze stream properties
#demuxer-lavf-probe-info=auto               # Probe stream information
#demuxer-lavf-probescore=                   # Minimum required probe score
#demuxer-lavf-allow-mimetype=yes            # Allow deriving the format the HTTP MIME type
#demuxer-lavf-format=                       # Force a specific demuxer
#demuxer-lavf-hacks=yes                     # Handle some formats differently
#demuxer-lavf-o=                            # AVOptions to libavformat demuxer
#demuxer-lavf-probesize=                    # Maximum amount of data to probe
#demuxer-lavf-buffersize=32768              # Size of the stream read buffer
#demuxer-lavf-linearize-timestamps=auto     # Linearize timestamp resets
#demuxer-lavf-propagate-opts=yes            # Propagate FFmpeg-level options

#demuxer-mkv-subtitle-preroll=              # Show embedded soft subtitles
#demuxer-mkv-subtitle-preroll-secs=         # How much data the demuxer should pre-read
#demuxer-mkv-subtitle-preroll-secs-index=   # Maximum amount of data to skip
#demuxer-mkv-probe-start-time=yes           # Check start time of mkv files
#demuxer-mkv-probe-video-duration=          # Seek to the end of the file and report duration

#demuxer-rawaudio-channels=stereo           # Number of channels
#demuxer-rawaudio-format=s16le              # Sample format
#demuxer-rawaudio-rate=44                   # Sample rate

#demuxer-rawvideo-fps=25.0                  # Video framerate
#demuxer-rawvideo-w=                        # Image width
#demuxer-rawvideo-h=                        # Image height
#demuxer-rawvideo-format=YV12               # Color space
#demuxer-rawvideo-codec=                    # Video codec
#demuxer-rawvideo-size=                     # Frame size

#demuxer-cue-codepage=                      # CUE sheet codepage
#demuxer-max-bytes=                         # How much the demuxer can buffer ahead
#demuxer-max-back-bytes=                    # How much the demuxer can preserve data
#demuxer-donate-buffer=yes                  # Let the back buffer use part of the forward buffer
#demuxer-seekable-cache=auto                # Whether seeking can use the demuxer cache
#demuxer-force-retry-on-eof=no              # Keep reading more packets each time the decoder dequeues a packet
#demuxer-thread=yes                         # Run demuxer in a separate thread
#demuxer-termination-timeout=0.1            # Number of seconds to wait to shut down the demuxer
#demuxer-readahead-secs=1                   # How much the demuxer should buffer ahead when in a different thread

#prefetch-playlist=no                       # Prefetch next playlist entry
#force-seekable=                            # Force seeking even if it's not possible
#demuxer-cache-wait=                        # Read all possible data before starting playback
#rar-list-all-volumes=no                    # Open al volumes of a multi-volume rar file


#########
# Input #
#########

#native-keyrepeat                           # Use system settings for keyrepeat
#input-ar-delay=                            # Delay before autorepeating a key
#input-ar-rate=                             # Number of key presses per second on autorepeat
#input-conf=~/.config/mpv/input.conf        # Input configuration file

#no-input-default-bindings                  # Disable default-level key bindings
#no-input-builtin-bindings                  # Disable loading of built-in key bindings

#input-cmdlist                              # Prints all unbounded commands
#input-doubleclick-time=300                 # Time to recognize two consecutive button presses as double-click
#input-keylist                              # Prints all unbounded keys
#input-key-fifo-size=7                      # Size of the FIFO key events buffer
#input-test                                 # Input test mode
#input-terminal                             # Enable reading key events from standard input
#input-ipc-server=                          # IPC support socket location
#input-ipc-client=                          # Connect single IPC client
#input-gamepad=no                           # SDL2 gamepad support
#input-cursor                               # Permit mpv to receive pointer events
no-input-right-alt-gr                       # Use AltGr as a special key different from Alt
#input-vo-keyboard=                         # Keyboard input for VOs


#######
# OSD #
#######

osc                                         # Load on-screen-controller
#osd-bar                                    # Display OSD bar
osd-on-seek=msg-bar                         # What is displayed on the OSD during seeks
#osd-duration=1000                          # Duration of the OSD message
#osd-font=sans-serif                        # OSD font
#osd-font-size=55                           # OSD font size

#osd-msg1=                                  # Show this message with OSD level 1
#osd-msg2=""                                #       ~~~                  level 2
#osd-msg3=""                                #       ~~~                  level 3
#osd-status-msg=                            # Custom OSD status text
#osd-playing-msg=                           # Show this message when playback starts
#osd-playing-msg-duration=                  # Duration of the 'osd-playing-msg'

#osd-bar-align-x=                           # Horizontal bar position
#osd-bar-align-y=                           # Vertical bar position
#osd-bar-w=                                 # Bar width
#osd-bar-h=                                 # Bar height

#osd-back-color=                            # OSD text background color
#osd-blur=0                                 # OSD blur
#osd-bold=                                  # Bold text
#osd-italic=                                # Italic text
#osd-border-color=                          # OSD font border color
#osd-border-size=3                          # OSD border size
#osd-color=                                 # OSD color
#osd-fractions                              # Show OSD time with fractions of seconds
#osd-level=                                 # OSD level

#osd-margin-x=25                            # Horizontal OSD margin
#osd-margin-y=22                            # Vertical OSD margin
#osd-align-x=left                           # Horizontal OSD position
#osd-align-y=top                            # Vertical OSD position
#osd-scale=                                 # OSD font size multiplier
#osd-scale-by-window=yes                    # Scale OSD with window
#osd-shadow-color=                          # OSD shadow color
#osd-shadow-offset=0                        # OSD shadow offset
#osd-spacing=0                              # OSD font spacing
#video-osd=yes                              # OSD rendering on the video
#osd-font-provider=                         # Font provider backend to use


##############
# Screenshot #
##############

#screenshot-format=jpg                      # Screenshot format
#screenshot-tag-colorspace=no               # Tag screenshot with the appropiate colorspace
#screenshot-high-bit-depth=yes              # Screenshot bit depth similar to video source
screenshot-template="%F-%p-%04n"            # Screenshot filename template
screenshot-directory=~/Media/screenshot     # Screenshot directory

#screenshot-jpeg-quality=90                 # JPEG quality level
#screenshot-jpeg-source-chroma=yes          # JPEG chroma similar to the video chroma
#screenshot-png-compression=7               # PNG compression level
#screenshot-png-filter=5                    # PNG compression filter
#screenshot-webp-lossless=no                # Use lossless WebP files
#screenshot-webp-quality=75                 # WebP quality level
#screenshot-webp-compression=4              # WebP compression level
#screenshot-jxl-distance=1.0                # JPEG XL Butteraugli distance
#screenshot-jxl-effort=3                    # JPEG XL compression effort
#screenshot-sw=no                           # Software rendering for screenshot


###################
# Software scaler #
###################

#sws-scaler=bicubic                         # Software scaler algorithm
#sws-lgb=                                   # Blur filter (luma)
#sws-cgb=                                   # Blur filter (chroma)
#sws-ls=                                    # Sharpen filter (luma)
#sws-cs=                                    # Sharpen filter (chroma)
#sws-chs=                                   # Chroma horizontal shifting
#sws-cvs=                                   # Chroma vertical shifting

#sws-bitexact=no                            # Disable optimizations
#sws-fast=no                                # Enable optimizations
#sws-allow-zimg=yes                         # Allow using zimg

#zimg-scaler=lanczos                        # Zimg luma scaler
#zimg-scaler-param-a=default                # Zimg scaler parameters
#zimg-scaler-param-b=default                #       ~~~
#zimg-scaler-chroma=bilinear                # Zimg scaler for chroma interpolation
#zimg-scaler-chroma-param-a=                # Zimg scaler parameters for chroma interpolation
#zimg-scaler-chroma-param-b=                #       ~~~
#zimg-dither=no                             # Dithering
#zimg-threads=auto                          # Maximum number of threads for scaling
#zimg-fast=yes                              # Allow optimizations

###################
# Audio resampler #
###################

#audio-resample-filter-size=16              # Resampler filter size
#audio-resample-phase-shift=10              # Number of polyphase entries in log2
#audio-resample-cutoff=                     # Cutoff frequency
#audio-resample-linear=no                   # Linearly interpolate between polyphase entries
#audio-normalize-downmix=no                 # Normalization if surround audio is downmixed to stereo
#audio-resample-max-output-size=40          # Maximumsize of audio frames filtered at once
#audio-swresample-o=                        # AVOptions on the SwrContext or AVAudioResampleContext


############
# Terminal #
############

#quiet                                      # Less verbose output
#really-quiet                               # Even less verbose output
#terminal                                   # Enable terminal, stdin, stdout and stderr
#no-msg-color                               # Disable colorful console output
#msg-level=status                           # Verbosity level

#term-osd=auto                              # Show OSD messages on console
#no-term-osd-bar                            # Print progress bar on terminal
term-osd-bar-chars=[=+-]                    # Progress bar characters
#term-playing-msg=                          # Message to show after starting playback
#term-status-msg=                           # Message to show during playback
#term-title=                                # Terminal title
#msg-module                                 # Prepend module name to each console message
#msg-time                                   # Prepend timing information to each console message


#########
# Cache #
#########

#cache=auto                                 # Cache
#no-cache                                   # Disable input stream caching
#cache-secs=                                # How many seconds to prefech
#cache-on-disk=                             # Use temporary files as cache
#cache-dir=                                 # Temporary cache files directory
#cache-pause=yes                            # Pause player when cache is empty
#cache-pause-wait=1                         # Number of seconds to wait for 'buffering'
#cache-pause-inital=no                      # Enter 'buffering' mode before starting playback
#cache-unlink-files=immediate               # Unlink cache files
#stream-buffer-size=128KB                   # Size of the low level stream buffer

#vd-queue-enable=no                         # Run video decoder on a separate thread
#ad-queue-enable=no                         # Run audio         ~~~
#vd-queue-max-bytes=                        # Maximum size of the video queue
#ad-queue-max-bytes=                        #       ~~~           audio queue
#vd-queue-max-samples=                      # Maximum number of frames of the queue
#ad-queue-max-samples=                      #       ~~~         samples of the queue
#vd-queue-max-secs=                         # Maximum number of seconds of media in the queue
#ad-queue-max-secs=                         #       ~~~


###########
# Network #
###########

#user-agent=                                # User agent for HTTP streaming
cookies                                     # Support cookies
#http-header-fields=                        # Custom HTTP fields
#http-proxy=                                # HTTP/HTTPS proxy

#tls-ca-file=                               # TLS certificate authority database file
#tls-verify                                 # Verify peer certificate
#tls-cert-file=                             # A file containing a certificate to use in the handshake with the peer
#tls-key-file=                              # A file containing the private key for the certificate

#referrer=                                  # Referrer path or URL for HTTP requests
#network-timeout=60                         # Network timeout
#rtps-transport=tcp                         # RTSP transport method
#hls-bitrate=max                            # HLS stream to select


#######
# DVB #
#######

#dvbin-prog=                                # Programe to tune to
#dvbin-card=0                               # Card number
#dvbin-file=~/.config/mpv/channels.conf     # Channels list file
#dvbin-timeout=30                           # Frequency tune timeout
#dvbin-full-transponder=no                  # Only tune frequency and pass full transponder to demuxer
#dvbin-channel-switch-offset=               # Offset for channel switching


#############################
# ALSA audio output options #
#############################

#alsa-resample=no                           # Alsa resampling plugin
#alsa-mixer-device=default                  # Mixer device to use
#alsa-mixer-name=Master                     # Name of the mixer element
#alsa-mixer-index=0                         # Mixer index channel
#alsa-non-interleaved                       # Allow output of non-interleaved formats
#alsa-ignore-chmap                          # Don't read or set the channel map of the ALSA device
#alsa-buffer-time=                          # Requested buffer time
#alsa-periods=                              # Number of periods requested from the ALSA API


########################
# GPU renderer options #
########################

scale=ewa_lanczossharp                      # Upscaling filter function
cscale=spline36                             # Upscaling chroma filter function
dscale=mitchell                             # Downscaling filter function
#tscale=mitchell                            # Temporal axis filter function

#scale-param1=default                       # Filter parameters
#scale-param2=default                       #       ~~~
#cscale-param1=default                      #       ~~~
#cscale-param2=default                      #       ~~~
#dscale-param1=default                      #       ~~~
#dscale-param2=default                      #       ~~~
#tscale-param1=default                      #       ~~~
#tscale-param2=default                      #       ~~~

#scale-blur=0                               # Kernel scaling factor
#cscale-blur=0                              #       ~~~
#dscale-blur=0                              #       ~~~
#tscale-blur=0                              #       ~~~
#scale-wblur=0                              # Window scaling factor
#cscale-wblur=0                             #       ~~~
#dscale-wblur=0                             #       ~~~
#tscale-wblur=0                             #       ~~~

#scale-clamp=0.0                            # Weight bias to multiply to negative coefficients
#cscale-clamp=0.0                           #       ~~~
#dscale-clamp=0.0                           #       ~~~
#tscale-clamp=1.0                           #       ~~~

#scale-cutoff=0.001                         # Cut off the filter kernel prematurely
#cscale-cutoff=0.001                        #       ~~~
#dscale-cutoff=0.001                        #       ~~~
#dscale-cutoff=0.001                        #       ~~~

#scale-taper=0                              # Kernel taper factor
#cscale-taper=0                             #       ~~~
#dscale-taper=0                             #       ~~~
#tscale-taper=0                             #       ~~~
#scale-wtaper=0                             # Window taper factor
#cscale-wtaper=0                            #       ~~~
#dscale-wtaper=0                            #       ~~~
#tscale-wtaper=0                            #       ~~~

#scale-radius=                              # Radius for tunable filters
#cscale-radius=                             #       ~~~
#dscale-radius=                             #       ~~~
#tscale-radius=                             #       ~~~

#scale-antiring=0.0                         # Antiringing strength
#cscale-antiring=0.0                        #       ~~~
#dscale-antiring=0.0                        #       ~~~
#tscale-antiring=0.0                        #       ~~~

#scale-window=                              # Custom window function for the kernel
#cscale-window=                             #       ~~~
#dscale-window=                             #       ~~~
#tscale-window=                             #       ~~~

#scale-wparam=default                       # Window function parameter
#cscale-wparam=default                      #       ~~~
#dscale-wparam=default                      #       ~~~
#tscale-wparam=default                      #       ~~~

#scaler-lut-size=6                          # Size of the lookup texture
#scaler-resizes-only                        # Disable scaler if image is not resized
#correct-downscaling                        # Extend convolution based filters size when downscaling
#linear-downscaling                         # Downscale linearly
#linear-upscaling                           # Upscale linearly
#sigmoid-upscaling                          # Upscale using sigmoid function
#sigmoid-center=0.75                        # Center of the sigmoid curve
#sigmoid-slope=6.5                          # Slope of the sigmoid curve

#interpolation                              # Reduce stuttering
#interpolation-threshold=0.01               # Interpolation activation threshold
#interpolation-preserve                     # Preserve previously interpolated frames
#opengl-pbo                                 # Enable use of PBO's
#dither-depth=no                            # Dither target depth
#dither-size-fruit=6                        # Size of the dither matrix
#temporal-dither                            # Enable temporal dithering
#temporal-dither-period=1                   # How often to update the dithering pattern
#error-diffusion=sierra-lite                # Error diffusion kernel
#gpu-debug                                  # Enable GPU debugging
#opengl-swapinterval=1                      # Interval in displayed frames between two buffer swaps

#vulkan-device=                             # Vulkan device
#vulkan-swap-mode=auto                      # Presentation mode of the vulkan swapchain
#vulkan-queue-count=1                       # Number of VkQueues used for rendering
#vulkan-async-transfer                      # Enable async transfer queues
#vulkan-async-compute                       # Enable async compute queues
#vulkan-disable-events                      # Disable VkEvents
#vulkan-display-display=                    # Index of the display
#vulkan-display-mode=                       # Index of the display mode
#vulkan-display-plane=                      # Index of the plane

#d3d11-exclusive-fs=no                      # Switches D3D11 swap chain state to 'fullscreen' when requested
#d3d11-warp=auto                            # Use WARP
#d3d11-feature-level=                       # Specific D3D11 feature level
#d3d11-flip=yes                             # Flip-model presentation
#d3d11-sync-interval=1                      # Schedule each frame to be presente for this number of VBlank intervals
#d3d11-adapter=                             # D3D11 adapter
#d3d11-output-format=auto                   # D3D11 output format
#d3d11-output-csp=auto                      # D3D11 output color space
#d3d11va-zero-copy=                         # Do not copy by sampling directly from the decoder image

#wayland-app-id=mpv                         # Wayland client app id
#wayland-configure-bounds=yes               # Controls wheter mpv opts into the configure bounds event
#wayland-disable-vsync=no                   # Disable internal vsync
#wayland-edge-pixels-pointer=10             # Size of an edge border to initiate client side resize events in contexts with the mouse
#wayland-edge-pixels-touch=32               #       ~~~                                                                with touch events

#spirv-compiler=auto                        # Compiler used to translate GLSL to SPIR-V
#glsl-shader=                               # Custom GLSL hooks
#glsl-shaders=                              #       ~~~

#deband                                     # Enable the debanding algorithm
#deband-iterations=1                        # Number of debanding steps per sample
#deband-threshold=32                        # Debanding filter's cut-off threshold
#deband-range=16                            # Debanding filter's initial radius
#deband-grain=48                            # Extra image noise
#sharpen=                                   # Image sharpness

#opengl-glfinish                            # Call glFinish() before swapping buffers
#opengl-waitvsync                           # Call glXWaitVideoSyncSGI() after each buffer swap

#gpu-sw                                     # Continue even if a software renderer is detected
#gpu-context=auto                           # GPU context
#gpu-api=auto                               # Graphics APIs
#opengl-es=auto                             # Controls which type of OpenGL context will be accepted
#fbo-format=auto                            # Internal FBOs texture format
#gamma-factor=1.1                           # Raw gamma factor
#image-lut=                                 # Custom LUT file to apply during image decoding
#image-lut-type=auto                        # Interpolation of LUT color values

#target-colorspace-hint                     # Auto configure output colorspace
#target-prim=auto                           # Primaries of the display
#target-trc=auto                            # Transfer characteristics of the display
#target-peak=auto                           # Measured peak brightness of the output display
#target-lut=                                # Custom LUT file to apply before display on-screen

#tone-mapping=auto                          # Tone-mapping algorithm
#tone-mapping-param=default                 # Tone-mapping parameters
#inverse-tone-mapping                       # Inverse tone mapping
#tone-mapping-crosstalk=0.04                # Apply an extra crosstalk matrix before tone mapping
#tone-mapping-max-boost=1.0                 # Upper limit for tone mapping
#tone-mapping-mode=auto                     # Controls how tone mapping is applied
#gamut-mapping-mode=auto                    # Algorithm used for reducing the gamut of images

#hdr-compute-peak=auto                      # Compute the HDR peak and frame average brightness per-frame
#allow-delayed-peak-detect                  # Allow delaying the detected peak by a frame
#hdr-peak-decay-rate=100.0                  # Decay rate used for the HDR peak detection algorithm
#hdr-scene-threshold-low=5.5                # Lower threshold for brightness difference
#hdr-scene-threshold-high=10.0              # Upper         ~~~

#use-embedded-icc-profile                   # Load the embedded ICC profile
#icc-profile=                               # ICC profile to use
#icc-profile-auto                           # Auto select ICC profile
#icc-cache-dir=                             # ICC cache directory
#icc-intent=1                               # ICC intent
#icc-3dlut-size=64x64x64                    # 3D LUT size
#icc-force-contrast=no                      # Override the target device's detected contrast ratio

#lut=                                       # Custom LUT file to apply to the colors
#lut-type=auto                              # Controls the interpretation values fed to and from the LUT file
#blend-subtitles=no                         # Blend subtitles directly onto upscaled video frames
#alpha=blend-tiles                          # What to do if the input has an alpha component
#opengl-rectangle-textures=no               # Force use of rectangle textures
#background=                                # Video background color
#gpu-tex-pad-x=                             # Enlarge horizontally the video source textures
#gpu-tex-pad-y=                             # Enlarge vertically        ~~~
#opengl-early-flush=auto                    # Call glFlush() after rendering a frame and before attempting to display it
#gpu-dumb-mode=auto                         # GPU dumb mode
#gpu-shader-cache-dir=                      # GLSL shaders cache directory


#################
# Miscellaneous #
#################

#display-tags=                              # List of tags to display on terminal
#mc=                                        # Maximum A-V sync correction per frame
#autosync=                                  # Gradually adjusts the A/V sync based on audio delay measurements
#video-timing-offset=0.050                  # How long before video display target time the video shoul be rendered
#video-sync=audio                           # How the player synchronizes audio and video
#video-sync-max-factor=5                    # Maxmium multiple to fit the video's FPS to the display's FPS
#video-sync-max-video-change=1              # Maximum speed difference that is applied to video
#video-sync-max-audio-change=0.125          # Maximum additional speed difference that is applied to audio

#mf-fps=1                                   # Framerate used when decoding from multiple PNG or JPEG
#mf-type=                                   # Input file type for mf://
#stream-dump=                               # Read file as a byte stream and write it to specified location
#stream-lavf-o=                             # AVOptions on streams opened with libavformat
#force-media-title=                         # Force the contents of the media-title
#external-files=                            # Files tracks to add
#external-file=                             # File tracks to add

#cover-art-files=                           # Files to use as cover art while playing audio
#cover-art-file=                            # File      ~~~
#cover-art-auto=exact                       # Whether to load the external cover art automatically
#cover-art-whitelist=yes                    # Whether to load filenames in an internal withelist

#autoload-files=yes                         # Automatically load external files
#stream-record=                             # File to write received data from the demuxer to
#lavfi-complex=                             # Set a 'complex' libavfilter filter
#metadata-codepage=utf-8                    # Codepage for various input metadata


#############
# Debugging #
#############

#unittest=                                  # Run internal unit test


############
# Encoding #
############

#o=                                         # Output filename of the encoding mode
#of=                                        # Output format
#ofopts=                                    # Output format options
#oac=                                       # Output audio codec
#oacopts=                                   # Output audio codec options
#ovc=                                       # Output video codec
#ovcopts=                                   # Output video codec options
#orawts=                                    # Copies input-pts to the output video
#no-ocopy-metadata                          # Do not copy metadata when encoding
#oset-metadata=                             # Metadata to include in the output file
#oremove-metadata=                          # Metadata to exclude from the output file
